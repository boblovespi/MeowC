let fac: i32 -> i32 := n \mt {
    1; if n = 0,
    n * fac(n - 1); otherwise.
};
/*
// test
*/
let main: 1 := []
{
    print "meow :3\n";
    print fac 7;
    print "\n";
    print double 500;
    print const();
};

let double: i32 -> i32 := n |-> [] 
{
    return n * 2;
};

let const: 1 -> u8 := a |-> 'h';

let tuple: i32 * i32 * i32 := (1, 2, 3);
let union: i32 + u8 := 1;
let unit: 1 := ();
/*
let num: i32 := 1 - 1 - 1;
let id: i32toi32 := x |-> x + 1;
let m: i8 := 'm';
*/
// let foo: bar := (A + B)(u + v);
// let curry: a -> a -> a := a;